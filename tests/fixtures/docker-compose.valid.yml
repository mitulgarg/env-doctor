version: '3.8'

services:
  gpu-app:
    image: nvidia/cuda:12.1.0-runtime-ubuntu22.04
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes:
      - ./app:/app
    command: python /app/main.py
